{
  "name": "node-mongo-collections-loader",
  "version": "0.0.1",
  "description": "Auth to every Mongodb databases used by the app and get an Object for each instance including an object for each collection",
  "main": "dist/index.js",
  "scripts": {
    "compile": "rm -rf dist/ && tsc",
    "compile:windows": "rmdir dist /s /q & tsc",
    "commit": "cz"
  },
  "repository": {
    "url": "https://github.com/pilerou/node-mongo-collections-loader"
  },
  "keywords": [
    "mongodb",
    "loader",
    "collection",
    "middleware",
    "nodejs"
  ],
  "author": "Pierre Le Roux <pierre@pastolpat.com",
  "license": "MIT",
  "dependencies": {
    "mongodb": "^3.6.3"
  },
  "devDependencies": {
    "ts-node": "^9.0.0",
    "typescript": "^4.0.3"
  }
}
